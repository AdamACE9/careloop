# CareLoop ProGuard rules.
# Nothing custom needed yet — release builds do not minify (see app/build.gradle.kts).
# TODO(backend): when Firebase + Gemini Live models are added, keep their data classes:
#   -keep class com.careloop.app.data.model.** { *; }
